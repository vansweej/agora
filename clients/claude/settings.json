{
  "_comment": "Reference allow-list for colleagues. Jan's corporate-managed ~/.claude/settings.json is left untouched; Home Manager deploys settings.local.json as ~/.claude/settings.local.json instead. apm never touches either settings file. The coordinator stores directly, so a denied write simply does not persist; cerebrum_forget is included in Jan's local override because plan-index replacement and session sweep require it.",
  "permissions": {
    "allow": [
      "mcp__cerebrum__cerebrum_recall",
      "mcp__cerebrum__cerebrum_recall_by_scope",
      "mcp__cerebrum__cerebrum_remember",
      "mcp__cerebrum__cerebrum_memorize"
    ]
  }
}
